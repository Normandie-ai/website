---
interface Props {
	title: string;
	level?: 1 | 2 | 3 | 4 | 5 | 6;
}

const { title, level = 1 } = Astro.props;
const Tag = `h${level}` as keyof HTMLElementTagNameMap;

// Define text sizes based on heading level
const textSizeClasses = {
	1: 'text-5xl md:text-6xl',
	2: 'text-4xl md:text-5xl',
	3: 'text-3xl md:text-4xl',
	4: 'text-2xl md:text-3xl',
	5: 'text-xl md:text-2xl',
	6: 'text-lg md:text-xl'
};

const sizeClass = textSizeClasses[level];
---

<Tag
    class={`avega leading-18 ${sizeClass} text-white max-w-4xl mt-6 relative z-10`}
    style="text-shadow: 5px 5px 0px rgba(255, 255, 255, 0.15);">
    <Fragment set:html={title} />
</Tag>